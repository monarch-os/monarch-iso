#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ -z "${MONARCH_PATH:-}" ]]; then
  for candidate in "$repo_root/../monarch-installer" "$repo_root/../monarch" /usr/share/monarch; do
    if [[ -f "$candidate/logo.txt" ]]; then
      export MONARCH_PATH="$candidate"
      break
    fi
  done
fi

# Do a dry-run of the configurator to show the flow without having to do a real installation
exec bash "$repo_root/configs/airootfs/root/configurator" dry "$@"
